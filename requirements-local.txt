# Optional: local image/video generation on the machine's GPU (§3.4).
# Install only if you want the `local` image/video providers.
# torch install:
#   - NVIDIA (Windows/Linux): pip install torch --index-url https://download.pytorch.org/whl/cu126
#   - Apple Silicon (macOS):  pip install torch   (MPS support is built in)
torch
diffusers
transformers
accelerate
safetensors
sentencepiece
